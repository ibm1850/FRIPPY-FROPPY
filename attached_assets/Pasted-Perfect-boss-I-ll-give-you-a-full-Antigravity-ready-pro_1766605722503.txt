Perfect boss 😎 I’ll give you a **full Antigravity-ready prompt** that tells the AI to **install everything automatically** on Windows (Node.js, npm, MySQL), set up the database, and generate a **fully functional Frippy Froppy webshop**. You just paste it and let it run.

---

## 📝 COPY-PASTE PROMPT FOR ANTIGRAVITY

**Prompt:**

---

You are going to create a **fully functional e-commerce website** called **“Frippy Froppy”**, a modern vintage clothing store. The user has **nothing installed** on their Windows machine. You are responsible for **installing everything needed automatically**, setting up the environment, and generating the full working project.

### 0️⃣ Automatic Environment Setup (Windows)

1. Install **Node.js (LTS version)** if not already installed.
2. Install **npm** (comes with Node.js).
3. Install **MySQL / MariaDB** if not installed.
4. Ensure **Windows PATH** is configured so `node`, `npm`, and `mysql` commands work.
5. Optional: Install **Git** if missing.

You must **automatically run these installations** via script or commands without user intervention.

---

### 1️⃣ Project Setup

* Create project folders:

  ```
  frippy-froppy/
    ├─ client/   → React frontend
    ├─ server/   → Node.js + Express backend
    └─ db/       → SQL scripts
  ```
* Create `.env` files:

  * Backend: database credentials + JWT secret
  * Frontend: API base URL

---

### 2️⃣ Tech Stack

* Frontend: **React.js** with hooks + **Tailwind CSS**
* Backend: **Node.js + Express**
* Database: **MySQL**
* Authentication: **JWT** for admin
* Image uploads: store URLs in database
* Responsive: PC, iPhone, Android
* Currency: **TND**

---

### 3️⃣ Database (MySQL)

Automatically create these tables:

1. **products** → id, name, price, image, description
2. **orders** → id, client_name, client_surname, address, postal_code, city, phone, total_price, status (“pending”), created_at
3. **order_items** → id, order_id, product_id, quantity
4. **admin** → id, email, password (hashed with bcrypt)

* Insert **sample admin credentials**:

  * Email: `admin@frippyfroppy.com`
  * Password: `Admin123` (hashed)

---

### 4️⃣ Frontend Pages

1. **Home Page**

   * Header: *Welcome to Frippy Froppy*
   * Subtitle: *Vintage vibes. Modern style*
   * Button: **Shop Now** → Shop page
   * Featured products grid

2. **Shop Page**

   * Display products from DB: image, name, price
   * **Add to Cart button**

3. **Cart Page**

   * Show products, quantity management
   * Subtotal + delivery fee 8 TND + total price
   * Checkout button

4. **Checkout Page**

   * Form: Nom, Prénom, Adresse, Code postal, Ville / Place, Numéro téléphone
   * On submit → save order and client info in DB
   * Confirmation message

5. **Admin Panel**

   * Admin login (email + password)
   * Dashboard:

     * Add/Edit/Delete products
     * View all orders + client info

* Components: Navbar, Footer, ProductCard, CartItem
* Styling: Tailwind CSS, minimalistic, rounded cards, hover animations, colors: black, white, beige, font: Inter/Poppins

---

### 5️⃣ Backend Routes

* `/api/products` → GET all, POST add (admin), PUT edit (admin), DELETE product (admin)

* `/api/orders` → POST create order, GET all orders (admin)

* `/api/admin/login` → POST login → JWT token

* Middleware: JWT authentication for admin, input validation, error handling

---

### 6️⃣ Functional Requirements

1. Clients can: browse, add to cart, checkout (save order + 8 TND delivery)
2. Admin can: login, manage products, view orders
3. Security: hash admin password, validate inputs, protect admin routes
4. Images: upload product images, store URL in DB
5. Fully responsive design

---

### 7️⃣ AI Instructions

* **Automatically install Node.js, npm, MySQL if missing**
* **Install frontend and backend dependencies** (`npm install`)
* **Generate all frontend and backend code fully working**
* **Create SQL scripts and initialize database automatically**
* **Include instructions or scripts to run the server and frontend automatically**
* **Ensure site works out of the box** for both client and admin

---

Boss 😎, this prompt literally tells the AI: **“you do everything, even install Node and MySQL”**, so you don’t have to touch anything.

If you want, I can make a **ready-to-paste ultra version** that also **includes scripts for automatic installation on Windows**, so after paste it, Antigravity will literally install Node, MySQL, and run the site automatically.

Do you want me to do that next?
